Review the following TypeScript code for statistical correctness (ANOVA) and PDF export quality.
Focus on:
1. Use of Welch ANOVA vs Classic ANOVA based on Levene's test.
2. Games-Howell vs Tukey HSD logic.
3. Partial Eta Squared calculation in 2-Way ANOVA.
4. Vietnamese font handling in jsPDF using WebR v3 VFS paths.

--- CODE EXCERPT 1 (ANOVA 1-WAY) ---
if (lev_p < 0.05) {
    w_res <- oneway.test(vals ~ grps, var.equal = FALSE);
    # ... Games-Howell manual calculation ...
    df_gh <- (vars[idx1]/ns[idx1] + vars[idx2]/ns[idx2])^2 / 
             ((vars[idx1]/ns[idx1])^2/(ns[idx1]-1) + (vars[idx2]/ns[idx2])^2/(ns[idx2]-1));
    q <- abs(md)/se * sqrt(2);
    c_p <- ptukey(q, k, df_gh, lower.tail=FALSE);
} else {
    # ... Tukey HSD ...
    tk <- TukeyHSD(mod);
}

--- CODE EXCERPT 2 (ANOVA 2-WAY) ---
# Calculate Partial Eta Squared: SS_effect / (SS_effect + SS_error)
ss_error <- res[nrow(res), "Sum Sq"];
eta_p <- res[, "Sum Sq"] / (res[, "Sum Sq"] + ss_error);

--- CODE EXCERPT 3 (PDF FONT) ---
const paths = [
    '/webr_core_v3/vfs/usr/share/fonts/NotoSans-Regular.ttf',
    '/webr_core/vfs/usr/share/fonts/NotoSans-Regular.ttf'
];

Please provide a deep critique and suggest any academic improvements in Vietnamese.
